package com.popo.repository;

import com.popo.models.BuildingTypeDescription;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BuildingTypeDescriptionRepository extends JpaRepository<BuildingTypeDescription, Long> {

}
