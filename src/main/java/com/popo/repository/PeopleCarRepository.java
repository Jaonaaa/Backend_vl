package com.popo.repository; 

import com.popo.models.PeopleCar;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PeopleCarRepository extends JpaRepository<PeopleCar,Long> {

} 
